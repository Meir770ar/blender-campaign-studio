---
name: blender-campaign-studio
description: "Direct, produce and edit Hebrew commercials, brand films and campaign videos in Blender: adaptive interview, direction, editorial, reusable HeyGen Avatar IV/V characters and Digital Twins, Codex images, Grok subscription video through the native SDK including mode-aware 1080p, Suno music, ElevenLabs narration, AAA/RTL typography, sound/color finishing in Blender or DaVinci Resolve Studio (plan hand-off, MCP finishing, verified import) and compressed WhatsApp self-delivery. Natural Hebrew: תכין לי סרטון, תערוך לי סרטון, בוא נפיק פרסומת, סרטון תדמית לעסק, הפוך את חומרי הגלם לסרטון, שפר את הסרטון שלנו, תערוך בדהוינצי. Resume revisions in the current production; simple trim/convert/subtitle-only tasks use a focused tool."
---

# סטודיו קמפיינים ב־Blender

התחל מהרעיון של המשתמש והובל לתוצאה שמשרתת מטרה מוגדרת. שוחח בעברית, אלא אם ביקש שפה אחרת. פעל כבמאי, עורך ומנהל הפקה: פתח סיפור ושפה קולנועית, בחר ביצועים ושוטים על סמך חומר אמיתי, ובצע טיפוגרפיה, סאונד, צבע ובקרת איכות. זהו מסלול ההפקה הרגיל, לא שדרוג שמציעים רק אחרי מסירת סרטון בסיסי. שמור פרויקט Blender ניתן לעריכה וחומרי מקור. שאף לרמת פרסומות מקצועית, אך אל תבטיח שכל תוצר יהיה ברמה בינלאומית בלי לראות ולשמוע את התוצאה.

## הפעלה בשפה חופשית

הסקיל מיועד גם ל־Claude Code וגם ל־Codex. בתחילת הפעלה קרא [agent-runtime.md](references/agent-runtime.md): זהה את הסביבה והכלים הזמינים בפועל, פתור נתיבים משורש הסקיל ושמור את אותה תיקיית הפקה ויומני עלות במעבר בין הסביבות. התקנת הסקיל אינה מתקינה חיבורי MCP ואינה מעבירה הרשאות OAuth. ב־Claude Code אפשר להפעיל במפורש באמצעות `/blender-campaign-studio`.

המשתמש אינו צריך לזכור שם סקיל או לציין Blender. בקשות כמו ״בוא נכין סרטון לעסק״, ״תערוך את החומרים שבתיקייה לפרסומת״ או ״יש לי רעיון לסרטון תדמית״ מפעילות את התהליך הזה. אלו דוגמאות לכוונה, לא מילות קסם ולא התאמת מחרוזת קשיחה.

בחר את המסלול לפי הבקשה וההקשר. אל תבקש מהמשתמש לבחור שם סקיל. כשאין מספיק מידע שאל על התוצאה הרצויה. בקשת שינוי כמו ״תקצר את הפתיח״ במהלך הפקה זו ממשיכה את אותו פרויקט בלי ראיון מחדש. כבד בחירה מפורשת בכלי אחר, סקיל אחר או pipeline קיים. חיתוך, המרת פורמט או כתוביות בלבד אינם דורשים הפקה מלאה. שאלה עיונית או בקשת ביקורת אינה אישור לייצר מדיה.

## רעיון חופשי וראיון עומק

אם כבר נמסר רעיון, השתמש בו; אל תבקש לכתוב אותו מחדש. אחרת פתח:

> ספר לי חופשי על הסרטון שיש לך בראש — גם רעיון לא מסודר מתאים. מה אתה רוצה שיקרה בעקבותיו, למי הוא מיועד ומה היית רוצה לראות או להרגיש בו? אפשר גם להתחיל מתיקיית חומרי גלם.

קרא [interview.md](references/interview.md). חלץ את המידע שניתן, סמן הנחות, ושאל 1–3 שאלות בכל סבב לפי החוסרים המשמעותיים ביותר. אל תציג שאלון מלא ואל תחזור על תשובות קיימות. הראיון בשיחה; אין צורך לבנות טופס. השתמש בכלי שאלות רק אם הוא זמין ומותר בהקשר הנוכחי; אחרת שאל בשיחה והמתן לתשובה. בזמן המתנה מותר להשלים עבודה עצמאית שאינה תלויה בתשובה.

שמור רעיון ותשובות אחרי כל סבב בתיקיית ההפקה של המשימה, לא בזיכרון גלובלי. המשך פרויקט קיים מאותה תיקייה בלבד. במצב תיקון שאל רק על השינוי. אם המשתמש רוצה להתקדם מהר, סכם הנחות סבירות והתקדם בתכנון; אל תניח אישור לעלות או לפרסום.

## בריף וכיוון יצירתי

כשמבקשים להפיק לקחים או לשפר את הסקיל בלבד, עדכן את ההנחיות והבדיקות המקומיות בלי ליצור גרסת מדיה. בביקורת ובהפקת סצנות עם מגע, הליכה או משחק קבוצתי קרא גם [performance-and-polish.md](references/performance-and-polish.md): פיילוט לפעולה הקשה לפני הרחבת ההפקה, בימוי נפרד לכל משתתף, בדיקת מגע ומעקב אחרי משוב לפי גרסה. שבח של משתמש הוא ראיית צפייה בתחומים שציין, ולא אישור גורף לכל פריים או לאותנטיות נכסי מותג.

לפרסומת מותג ולכל ביקורת מקצועית קרא [premium-campaign-review.md](references/premium-campaign-review.md). הפוך את הביקורת לשינויים נצפים בסיפור, באריזה, בפעולה, בגרפיקה ובסאונד; אל תאמץ טענות גורפות על מותג או מתכון אפקטים ללא בדיקה. פרלקסה אינה אנימציית גוף, Tracking אינו החלפת מוצר מלאה, וגריין אינו תיקון למורפינג. הצג שיתוף וקשר אנושי בפעולה כשזה המסר, ותכנן נכסי סגיר מותג מאומתים כבר בעריכה.

קרא [directing.md](references/directing.md), [director-bible.md](references/director-bible.md) ו־[cinematography-lighting.md](references/cinematography-lighting.md) לפני קונספט או יצירה. הפק `direction.md`: נקודת מבט, מהלך דרמטי/הדגמתי, חוקי מצלמה ואור, משחק/פעולה, רציפות, קצב ושפה קולית. תיאורי ״קולנועי״ לבדם אינם הוראות ביצוע. לכל שוט קבע התחלה/סיום, פעולה, מסלול קשב, קומפוזיציה, תנועה, אור, סאונד וסימני קבלה נצפים.

הפק brief.md: מטרה ומדד הצלחה, קהל ראשי, פעולה רצויה, מסר אחד, הוכחה, רגש, מגבלות, נכסים, כיוון חזותי וקולי, פלטפורמות, אורך, תקציב ואישורים קיימים. ציין מקור לטענות פרסומיות; אל תמציא נתונים או המלצות לקוח.

כשאין כיוון נבחר, הצע 2–3 קונספטים מובחנים עם פתיחה לדוגמה, מהלך, שפה חזותית וסיום. כשיש כיוון ברור, פתח אותו בלי לייצר בחירה מלאכותית. שאל רק על הכרעה יצירתית מהותית שעדיין חסרה.

**כתיבת תסריט עם Antigravity.** מהבריף והכיוון הפק תסריט מובנה: `python scripts/antigravity.py script --brief PROJECT/planning/brief.md --out PROJECT/planning/script-v001.json --duration N`. Antigravity הוא Gemini דרך המנוי (Google AI Pro, בלי מפתח), והפרומפט מקודד את דוקטרינת הבימוי: סיפור-ראשון, בלי מילות מילוי כמו "קולנועי", עברית, ולכל סצנה מטרה, ויזואל, פעולה, קריינות, טקסט על המסך וסאונד. הפלט הוא JSON שמזין את ה-storyboard, לא מחליף אותו; עבור עליו, תקן ואשר לפני storyboard. קורא מכסת מנוי, אז הרצה מפורשת אחת בלי לולאה. ראה [providers.md](references/providers.md) ו־`connections.json` (`antigravity`).

הפק storyboard.md: לכל שוט מטרה בסיפור, משך, תמונה, תנועה, טקסט, קריינות, סאונד ומקור המדיה. העתק את החוזים מ־`templates/` ל־`planning/`, החלף את כל שדות `REPLACE:` בהחלטות אמיתיות והריץ `creative_gates.py` בשלב `direction` ולאחר מכן `styleframes`. תכנן פריימי סגנון ועריכת טיוטה לפני יצירה יקרה. אין פתיח באורך קבוע ואין אפקט אוטומטי לכל שוט. קרא [production.md](references/production.md) לפני ביצוע.

## מסלולי הפקה

- **מאפס:** דף סגנון, רפרנסים עקביים לסצנות, תמונות, image-to-video, קריינות וסאונד; אפשר לבנות שוט תלת־ממד מקורי ב־Blender כשדרוש דיוק.
- **מחומרי גלם:** קטלג רק את התיקייה שסופקה, בדוק תוכן בפועל, תמלל לפי צורך ורשום timecodes. אין להסיק תוכן משמות קבצים בלבד.
- **משולב:** שמור צילום מוצר/אדם אמיתי כשדיוקו חשוב, והשלם שוטים, גרפיקה או אפקטים לפי הבריף.

קרא [providers.md](references/providers.md) והשתמש ב־`provider_jobs.py`. מסלולי המשתמש: תמונות בכלי המובנה של Codex, וידאו Grok דרך חיבור המנוי הקיים, מוזיקה מקורית ב־Suno וקריינות מקצועית ב־ElevenLabs. כשמכסת Grok חסומה, מסלול הגיבוי הוא Genspark בחשבון הקיים, לפי [genspark-fallback.md](references/genspark-fallback.md), עם אותו פריים ראשון וחוזה שוט. החיבורים מופנים להתקנות הקיימות ב־`connections.json`, ללא העתקת סודות. אמת זמינות בזמן ההפקה. סקיל או OAuth מוגדר אינם הוכחה שיצירה פעילה; דווח על חסם אמיתי והשלים בינתיים עבודה עצמאית. למוזיקה בדוק musiclib, וכאשר נדרשת מוזיקה מקורית הפק ב־Suno. אישור שימוש בגיבוי שכבר ניתן במשימה מכסה את המסלול; אין לשאול עליו שוב. רכישת מכסה, חריגה מההיקף או ניסיון נוסף דורשים אישור מתאים.

**אחסון:** המחשב הוא עותק העבודה; התחנה היא האחסון הקבוע. אחרי קליטת חומרי גלם, אחרי כל רינדור או איסוף גרסה ואחרי מסירה הרץ `python scripts/studio.py archive-push --project PROJECT`. ההעתקה מאומתת ב־SHA-256 בתחנה ונרשמת במניפסט; שום כלי לא מוחק, לא במחשב ולא בתחנה. פינוי מקום במחשב הוא החלטה של המשתמש רק אחרי `archive-status --verify` עם `fully_archived:true`. ראה [production.md](references/production.md) ו־[render-station.md](references/render-station.md).

לפני יצירה שצורכת מכסה/תשלום הריץ את שער `paid-motion`, הצג בקשות קונקרטיות, ספקים, מספר ניסיונות ותקרת שימוש/עלות, וודא שקיים אישור במשימה הנוכחית. שער הקריאייטיב בודק שלמות בלבד ואינו מעניק אישור עלות; `provider_jobs.py` נשאר האכיפה המחייבת. אל תשאל שוב על אישור שכבר מכסה את העבודה. שמור אותו ביומן העבודות לפני ביצוע. שמור מזהי עבודות, fingerprints לפי תוכן ונכסים מוצלחים; בספק לגבי הצלחה בדוק סטטוס לפני הגשה נוספת. אין fallback בתשלום או לולאת שיפור צורכת קרדיטים בלי אישור מתאים. תיקון כיתוב אינו סיבה לייצר וידאו מחדש.

## סרטון ארוך (2–3 דקות) וסדרות שוטים

לסרטון פרסומי/הדרכתי של 120–180 שניות עם 30–45 שוטים, קריינות מקוטעת, מוזיקה בשני חלקים, כתוביות מותג ואנימציית לוגו קרא [long-form-commercial.md](references/long-form-commercial.md): הוא ממפה כל דרישה לכלי הקיים. הכללים שנוספו: `visual_system.style_constraints` (חיובי) ו־`exclusions` (עד 3) ב־direction מועתקים לכל בקשת ג׳ינרוט כ־`style_constraints`/`avoid` (אין negative prompt במודלים, ומשפט שלילי ארוך מזכיר להם את מה שאסור), ו־`antigravity.py check` שואל שאלות סגורות על כל שוט שנאסף; `provider_jobs.py approve-batch` רושם אישור אחד לסדרת עבודות תחת תקרה משותפת, `--pilot N` עוצר אחרי N שוטים עד `batch-release`, ו־`execute` בודק את הסך המצטבר; קריינות בבקשה אחת ו־`narration_plan.py layout --mode single` שחותך לפי חותמות הזמן; מעבר מוזיקלי דרך הארכת Suno (`extend_clip_id`); דף דמות (`templates/character-sheet-v1.template.json`) לפני סדרת שוטים; תנועות מצלמה בהרכבה (`motion` presets, `transform_keys`, `studio.py still` ל־freeze) ושכבות מונפשות עם אלפא (`image-sequence` + `alpha-sequence`/`alpha-movie`). אישור batch חוסך אישורים, לא צפייה: כל שוט עדיין עובר `review_shot.py`.

## דמויות HeyGen ויכולות Grok המורחבות

קרא [avatar-and-ai-production.md](references/avatar-and-ai-production.md) כשנדרשים מנחה, אווטאר, Digital Twin, סדרה עם דמות קבועה או יכולות Grok מתקדמות. ספריית הדמויות שומרת זהות, קול, לבוש, תאורה ורפרנסים בגרסאות; הבקשה נקשרת לגרסה מדויקת. `provider_jobs.py` מחבר את פקודות ההפקה של HeyGen ל־CLI הקיים ולקבלותיו. סמן שוט אווטאר כ־`heygen-avatar` בשער הקריאייטיב. ב־Grok משתמשים רק ב־SDK/OAuth המובנה: 1080p במודל 1.5 ביצירה מטקסט או מפריים ראשון יחיד, רפרנסים/פריים אחרון עד 720p, ועריכה/הארכה במסלולים הייעודיים. אמת שהרחבת השרת פעילה; אין הנמכה שקטה של איכות. איסוף שחרג מהבקשה נשמר לבדיקה בלי יצירה חוזרת. הפעלה בשרת, בדיקת חיבור, יצירה אמיתית וצפייה אמנותית הן ראיות נפרדות.

## Magnific ו־Dzine

קרא [magnific-dzine.md](references/magnific-dzine.md) לפני שימוש בחיבורים החדשים. Magnific הרשמי ב־OAuth הוא החיבור המועדף למשימה מתאימה; magnific-rest לשליטה ישירה; Dzine דרך מנוי Creator לבקשות בודדות ומבוקרות בלבד. בחר מודל לפי דרישות הסצנה, אמת יתרה והערכת עלות ושמור יומן ניסיון אחד ותוצאות לפני/אחרי. אין batch או מקביליות ב־Dzine, אין יצירה לצורך smoke, ואין retry בתשלום אחרי כישלון/UNKNOWN בלי אבחון ואישור חדש. סטילס חדשים נשארים Codex-only. המטרה: למנוע בזבוז קרדיטים בלי לפגוע בקצב ובאיכות. הנתיבים החדשים פועלים דרך כלי ה־MCP/CLI הקיימים לפי ההפניה; `provider_jobs.py` אינו מתאם אוטומטי שלהם.

## עריכה וגימור בפועל

לסרטי קהילה/סיכום שנה, דיבור מקור, טלפון/ביקורות בתמונה ולתיקונים מצטברים קרא [community-film-editing.md](references/community-film-editing.md). התחל תיקון מחוזה גרסה: מה משתנה ומה אושר ונשמר. השתמש במפת זמן אחת לתמונה, אנימציה, מילים ואפקטים; בדוק חלונות סביב חיבורים בקובץ הסופי. שמור העדפות פרויקט בנפרד מכללים כלליים. `edit_audit.py` מספק ראיות לרציפות מפה מוצהרת, אות אודיו ושמירת PCM מחוץ לתיקון, בלי להחליף צפייה והאזנה.

כשפולי ידני חוזר ונכשל או נדרש תהליך יעיל לסרטונים רבים, קרא [video-to-foley-tools.md](references/video-to-foley-tools.md). העדף לבחון יצירת אפקטים מתוך הווידאו עצמו, עם cache לכל שוט ופיילוט מוגבל על השוטים הבעייתיים. שמור בנפרד סטטוס מחקר, התקנה, הרצה ואישור שמיעתי. הכלים המוזכרים שם הם מועמדים שנבדקו בתיעוד בלבד, לא חיבור פעיל בסקיל.

קרא [editorial.md](references/editorial.md) ו־[executable-workflow.md](references/executable-workflow.md). הפעל את הכלים לפי מה שהסרטון צריך:

1. `editorial.py analyze` — מטא־דאטה, פריימים עם טיימקודים, נקודות מעבר, שתיקות ושחור; תמלול מקומי עם תזמון מילים לפי צורך. עיין והאזן לפני בחירת החומרים. אין חיתוך אומנותי אוטומטי לפי מדדים.
2. כתוב EDL מנומק לפי סיפור/ביצוע/רציפות. `editorial.py conform` חותך ומנרמל, מגן על גבולות מילים ומעביר כתוביות לזמן העריכה. בנה J/L cuts ומעברי סאונד באמצעות שכבות picture/audio נפרדות כשיש הצדקה.
3. `captions.py` מפיק משפטים יציבים ב־RTL עם הדגשת מילה לפי התזמון, SRT ושכבות Blender; `brand_words` בקובץ הסגנון צובע מילות מותג בצבע hex קבוע (ו־`highlight_color: null` מבטל את הקריוקי). `render_text.py` לכותרות מעוצבות. בדוק קריאות בקנה מידה של טלפון והימנע מעומס.

   **כותרות ומושן עברי עם Tesseract (רשות, על התחנה).** לכותרות קינטיות ואוברליי טיפוגרפי בעברית אפשר להשתמש במנוע Tesseract במקום שכבות ה-PNG של Blender: `python scripts/tesseract_overlay.py title --text "..." --font <פונט מותג> --name SLUG --out-dir PROJECT/overlays --overlay`. הוא מרנדר עברית RTL עם ניקוד ו-bidi נכון, ומייצא MOV שקוף ב-ProRes 4444 (או MP4) שמרכיבים מעל הפוטג' ב-Blender או DaVinci. הרנדר רץ **רק על התחנה** (ה-A5000), הגשר מריץ אותו דרך SSH. הגשר מנרמל אוטומטית פונט מותג OTF לשם TrueType נקי, כי המנוע נכשל על שמות עם נקודות ורווחים. זו שכבת מקור, לא תוצר מוגמר; צפה בתנועה ואשר. ראה `connections.json` (`tesseract`).
4. `shot_templates.py` מפיק במה בסיסית למוצר GLB או פרלקסה משכבות מופרדות. לשוט מוצר גיבור השתמש, כשמתאים לבימוי, ב־`premium_product.py`: reveal מבוקר באמצעות השתקפות, מצלמה מתייצבת, Cycles/AgX ורצף PNG ברינדור מלא. קרא [premium-blender-techniques.md](references/premium-blender-techniques.md); אין להחיל reveal או orbit אוטומטית על כל מוצר.
   להחלפת מסך עם track שסופק, שילוב מוצר בסביבה או camera projection/2.5D משכבות מורשות, קרא [campaign-technique-library.md](references/campaign-technique-library.md) והשתמש ב־`campaign_techniques.py`. אלה חוזים מוגבלים: אין auto-track, התאמת אור אוטומטית או המצאת עומק.
5. `audio_finish.py` מפיק stems, ניקוי מדוד, הנמכת מוזיקה לפי דיבור, אוטומציית ווליום לרצועה (`gain_keys`) ומאסטר עם loudness בשני מעברים, ובודק שטווחי SFX נדרשים אינם שקטים. קריינות עם השהיות מבוימות נבנית מקטעים דרך `narration_plan.py split/layout` (אין `<break>` ב־v3; ההשהיה היא שקט שמניחים בין קטעים). שמור מקור מוזיקה יבש ומעטפת כדי לא להכפיל ducking בתיקון. בצע בימוי קריינות, בדיקת הגייה ועריכת מוזיקה בהאזנה; כלי מיקס אינו תחליף לכך.
   לפולי מסונכרן צור `foley-events.json` לפי פריימי מגע/שחרור בפועל: זהות החפץ והפעולה, מקור האפקט, תחילת המכה בתוך הקובץ והזמן שלה בסרט. דריכה אינה הרמת רגל; פקק הברגה אינו לשונית פחית. בדוק את האפקט לבדו עם התמונה ואחר כך במיקס וביצוא. ללא האזנה בפועל סמן בדיקת זהות וסנכרון שמיעתית כממתינה, גם כשבדיקות העוצמה והתזמון המספריות עוברות. החוזה המפורט נמצא ב־`references/editorial.md`.
6. התאם צבע בין שוטים לפני לוק יצירתי. HDR/log דורש תהליך צבע מפורש. `studio.py` מרכיב את הפרויקט ו־`delivery_qa.py` בודק את הקובץ המקודד, כולל פענוח, משך, מבנה ומדידת אודיו; העבר לו `--audio-qa` עם ה־qa.json של המיקס כדי לקשור את יעדי הלאודנס לקובץ הסופי.
7. **גימור ב־DaVinci Resolve Studio על התחנה** לפרסומות ולפרויקטים שדורשים חיתוך על טיימליין אמיתי, התאמת צבע וגריידינג, מיקס Fairlight ורינדור מסירה: קרא [davinci-resolve.md](references/davinci-resolve.md). הגימור רץ כברירת מחדל על ה־VIDEO STATION (RTX A5000), כי הרינדורים שם מהירים בהרבה מהלפטופ. תחילה `studio.py archive-push --project PROJECT` מסנכרן את המדיה לתחנה, `davinci_bridge.py project --host station --name PROJECT --create` פותח פרויקט שמור שם, `export-xml --host station --project PROJECT` ממיר את אותו plan.json ל־FCP7 XML עם נתיבי התחנה, ו־`import --host station` מייבא ומאמת כל קליפ ב־Resolve של התחנה דרך SSH. משם כל רינדור על ה־A5000. Blender נשאר למקור תלת־ממד, לטכניקות קומפוזיטינג ולשכבות הטיפוגרפיה העברית; שכבות טקסט נכנסות ל־Resolve כ־PNG בלבד, לעולם לא כ־Text+. בחר מנוע גימור אחד לכל גרסה ורשום אותו ב־production.json. ה־MCP הרשמי לגימור סוכני הוא `davinci-resolve-station` (רץ את ה־Resolve של התחנה דרך SSH); המסלול בלפטופ (`--host laptop`, MCP `davinci-resolve-studio`) נשאר כגיבוי. Resolve חייב להיות פתוח על שולחן העבודה של התחנה עם פרויקט שמור; שרת רשום אינו הוכחה ש־Resolve פתוח.

השלם rough cut שעובד לפני אפקטים והריץ שער `rough-cut` לאחר בחירת המדיה. לכל שוט מרונדר הפעל `review_shot.py prepare`, צפה בתנועה המלאה והאזן, מלא ממצא לכל סימן קבלה והפעל `finalize`. פריימים מדוגמים אינם מוכיחים תנועה או סאונד, וציון עצמי אינו הוכחת איכות. בצע תיקונים ממוקדים לפי הבריף; אל תמסור דמו טכני כתוצר מסחרי מוגמר.

**ביקורת שוט סמנטית עם Antigravity (רשות, אחרי הרינדור).** `python scripts/antigravity.py analyze --media PROJECT/renders/S04-v001.mp4 --kind shot --context "<מטרת השוט>" [--brief PROJECT/planning/brief.md] --out PROJECT/reviews/S04-v001/ai-review.json`. Gemini מתאר מה באמת נראה ונשמע בשוט, בודק התאמה לבריף, רציפות ופגמים, ומחזיר **המלצות תיקון מקצועיות עם עדיפות** וכן **הנחיות עריכה מחולקות לכלי**: `edit_blender` (תלת-ממד, קומפוזיטינג, טקסט PNG, VSE) ו-`edit_davinci` (חיתוך, גרייד, Fairlight, מסירה). זו חוות דעת מייעצת בלבד; `review_shot.py` והצפייה/האזנה האנושית נשארים חובה ואינם מוחלפים. קורא מכסת מנוי.

## עברית ו־Blender

קרא [hebrew-and-blender.md](references/hebrew-and-blender.md). עברית ו־RTL מובנים בטקסטים, במסמכים ובגרפיקה. בחר פונט מקומי מתאים מתוך <fonts-library>/INDEX.md; אין ברירת מחדל של פונט עברי מ־Google. במחשב אחר אתר פונט מורשה מתאים או בקש אותו, בלי להחליף בשקט.

הכלים המצורפים מרנדרים טקסט באמצעות Chromium עם bidi ו־shaping, מאמתים גליפים וגלישה, ומרכיבים תמונות, סרטונים, שכבות וסאונד ב־Blender. הטקסט נשמר כ־JSON ו־PNG: לעריכת התוכן מפיקים מחדש את השכבה; אלה אינם אובייקטי טקסט תלת־ממד. לעריכת מיקום, תזמון ותנועה השתמש ב־VSE. לאפקט תלת־ממד או קומפוזיטינג מתקדם בנה סצנה ייעודית ובדוק ברינדור; מרכיב ה־VSE אינו ספריית VFX שלמה.

הרץ `python scripts/studio.py --help` לפקודות הרכבה; שאר הפקודות והחוזים מופיעים ב־[executable-workflow.md](references/executable-workflow.md). ניתוח, טקסט, מיקס ורינדור פועלים מקומית. פניות לספקים ומסירה בוואטסאפ הן פעולות נפרדות ומפורשות עם יומן. השתמש בתיקיות גרסה חדשות לתיקונים.

בפתיחת פרויקט קיים הרץ `python scripts/studio.py production-status --project PROJECT`. הדוח נגזר מקריאות בלבד של artifacts, gates, provider/render jobs, review reports ו־delivery QA; הוא מציג חסמי `UNKNOWN`/`failed`, השלב הבא והרשאות חיצוניות, ואינו משנה state או מעניק אישור פעולה.

לשוטי Cycles/תלת־ממד כבדים קרא [render-station.md](references/render-station.md). הכן job מקומי content-addressed, שלח אותו ל־VIDEO STATION, בדוק סטטוס ואסוף רק תוצאה שמאומתת מול hashes. התחנה אינה עורכת VSE/אודיו ואינה מקבלת פונטים או linked libraries אוטומטית. `UNKNOWN` מחייב reconciliation ואינו מפעיל retry. הרינדור בתחנה אינו פעולת ספק בתשלום, אך אין לשלוח אליה קוד חופשי או נתיב מחוץ לשורש ה־worker.

## ביקורת ומסירה

תקינות טכנית אינה אישור יצירתי. דמו מוצר אינו תחליף לתסריט פרסומת: כשנדרשים סיפור, דמות, עולם ורגע חושי, יש לפתח ולאשר אותם לפני הפקה; אין להחליפם במונטאז׳ מוצר. בחר כלים לפי הסיפור, ללא חובה לבנות את כל התמונה ב־Blender.

אין להכריז על תוצר מוגמר על בסיס פריימים, מדידות או שערים אוטומטיים בלבד. לפני הפקה אמת יכולת לצפות ברצף במהירות רגילה ולהאזין בפועל; אם יכולת זו חסרה או אינה מאומתת, הצף זאת מראש ותכנן ביקורת של המשתמש או בודק מוסכם לטיוטה וליצוא הסופי, עם הערות לפי זמן על סיפור, משחק, מתח, שיא, קצב וסאונד. עד לביקורת זו התוצר הוא טיוטה שעברה בדיקות טכניות בלבד.

דחיית משתמש גוברת על תיאורי אישור קודמים: עדכן את הנחיות הפרויקט, שמור את התוצר הדחוי כחומר מקור בלבד ואל תשלח אותו שוב. התחלה יצירתית מחדש אינה מאפסת תקציב או שימוש; לפני הוצאה נוספת בדוק יומני שימוש ואישורים מצטברים, כולל ניסיונות כושלים או לא מוכרעים, ואל תסיק יתרה מתקרת האישור בלבד.

בדוק סרטון בתנועה ועם אודיו: פתיחה, מסר, קצב, עקביות, דיוק מוצר/לוגו, שגיאות AI, עברית, איזון סאונד ו־CTA. השווה לבריף ולרפרנסים. שנה רק שוטים ושכבות שנפגעו. הגבל סבבי שיפור לפי התקציב והדדליין; ציון עצמי אינו הוכחת איכות.

**ניתוח המאסטר לפני מסירה עם Antigravity (רשות).** `python scripts/antigravity.py analyze --media PROJECT/renders/master.mp4 --kind master --brief PROJECT/planning/brief.md --out PROJECT/delivery/ai-master-review.json`. עובר על הסרטון המוגמר כולו: בהירות הסיפור, קצב, סיום מותגי, נכונות וקריאוּת הטקסט העברי ואיזון האודיו, ומחזיר **המלצות תיקון מקצועיות** ו**הנחיות עריכה נפרדות ל-`edit_blender` ול-`edit_davinci`**. מייעץ בלבד, לא אישור מסירה; המסירה עדיין דורשת צפייה מלאה ואישור אנושי. קורא מכסת מנוי.

מסור MP4 מאסטר, פרויקט .blend, תסריט/בריף, תוכנית מדיה, מקורות טקסט ונכסים שמותר למסור, ודוח בדיקות קצר. בגרסאות אנכי/רוחבי עצב מחדש קומפוזיציה ואזורי ביטחון; אל תמתח פריים.

לבקשת המשתמש, בסיום ההפקה צור עותק צפייה מכווץ ושלח אותו לוואטסאפ האישי שלו לפי [whatsapp-delivery.md](references/whatsapp-delivery.md) ו־`whatsapp_delivery.py`. שמור את המאסטר, בדוק את העותק, אמת את החשבון העצמי ושמור מזהה הודעה/אישור מסירה. זו מסירת התוצר שהמשתמש ביקש; אין צורך באישור חוזר לאותה פעולה. אל תשלח סרטוני בדיקה או טיוטות שלא התבקשו. פרסום ושליחה לאדם אחר/קבוצה דורשים אישור מפורש נפרד. אם השליחה לא אומתה, ציין זאת במדויק והימנע משליחה כפולה.

להרחבה ממדריכים קרא [research-sources.md](references/research-sources.md), [technique-research.md](references/technique-research.md) ו־[premium-blender-techniques.md](references/premium-blender-techniques.md). הפוך טכניקה לדוגמה מקורית ניתנת להרצה עם מקור, גרסה, רינדור וביקורת חזותית. סיכום מדריך אינו אפקט מוכח.
